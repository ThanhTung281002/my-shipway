# my shipway microservice 

Chỉ làm service cho phần được giao thôi nhưng muốn thử với cấu trúc của microservice để thử cách kết nối FE và API bên BE. 

FE làm trong thư mục `frontend/src/pages/scan-drivers`
BE làm trong thư mục `services/scan-drivers`